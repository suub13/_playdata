{{>layout/header}}
<div class="container">
    <div class="row">
        {{>layout/mypage-menu}}
        <div class="col-md-9">
            <div class="card">
                <div class="card-body">
                    <div class="form-group" style="display: none;">
                        <label for="id">회원 번호</label>
                        <input type="text" class="form-control" id="id" value="{{hiker.id}}" readonly>
                        <label for="id">회원 아이디</label>
                        <input type="text" class="form-control" id="userid" value="{{hiker.userid}}" readonly>
                    </div>

                    <li class="list-group-item">
                        탈퇴하고 싶으십니까? 탈퇴시 모든 정보는 사라지게 됩니다.
                    </li>
                    <button id="btn-delete" class="btn btn-danger" type="button">회원 탈퇴</button>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/app/register.js"></script>
{{>layout/footer}}